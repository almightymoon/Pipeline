# 📋 Repositories to Process
# 
# Add any GitHub repository URLs here and push to trigger the pipeline!
# The pipeline will automatically detect if it's a dataset or code repo.
#
# Format:
#   - repo: GitHub repository URL
#     branch: Branch name (optional, defaults to 'main')
#     type: auto/dataset/code (optional, auto-detects if not specified)
#     format: json/csv/parquet (for datasets, auto-detects if not specified)

repositories:
  # Example: Your dataset repository
  # - repo: https://github.com/your-username/your-dataset.git
  #   branch: main
  #   type: dataset
  #   format: json
  
  # Example: Your code repository
  # - repo: https://github.com/your-username/your-app.git
  #   branch: develop
  #   type: code
  
  # Add your repositories below:
  
  # Real test: Small iris dataset from GitHub
  - repo: https://github.com/mwaskom/seaborn-data.git
    branch: master
    type: dataset
    format: csv
  

