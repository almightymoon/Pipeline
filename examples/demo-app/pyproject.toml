[project]
name = "demo-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "flask==3.0.3",
  "gunicorn==22.0.0",
  "prometheus-client==0.26.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
